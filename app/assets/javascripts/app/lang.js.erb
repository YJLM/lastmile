var lang = {};
$.extend(lang, {
	init: function(){
		this.fmx.parent = this.views.parent = this;
	},
	messages: {
		error: '<%= t 'app.messages.error' %>'
	},
	views: {
		parent: null
	},
	fmx: {
		parent: null,
		notification: function(){
			var opts = {};
			$.extend(opts, {
				
			});
			return opts;
		},
		sprite: function(){
			var opts = {};
			$.extend(opts, {
				
			});
			return opts;
		},
		tabs: function(){
			var opts = {};
			$.extend(opts, {
				
			});
			return opts;
		},
		slider: function(){
			var opts = {};
			$.extend(opts, {
				labels: {
					not_found: '<%= t 'fmx.front.slider.labels.not_found' %>'
				}
			});
			return opts;
		},
		coord_sprite: function(){
			var opts = {};
			$.extend(opts, {
				
			});
			return opts;
		}
	}
});
lang.init();