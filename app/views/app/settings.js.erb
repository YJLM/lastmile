var config = {};
$.extend(config, {
	init: function(){
		this.fmx.parent = this;
	},
	fmx: {
		parent: null,
		notification: function(){
			var opts = {};
			$.extend(opts, {
				
			}, lang.fmx.notification());
			return opts;
		},
		sprite: function(){
			var opts = {};
			$.extend(opts, {
				
			}, lang.fmx.sprite());
			return opts;
		},
		tabs: function(){
			var opts = {};
			$.extend(opts, {
				
			}, lang.fmx.tabs());
			return opts;
		},
		slider: function(){
			var opts = {};
			$.extend(opts, {
				intervals: {
					color: 500,
					idle: 5000,
					ttl: 5000
				}
			}, lang.fmx.slider());
			return opts;
		},
		coord_sprite: function(){
			var opts = {};
			$.extend(opts, {
				
			}, lang.fmx.coord_sprite());
			return opts;
		}
	}
});
config.init();