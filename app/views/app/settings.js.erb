var config = {};
$.extend(config, {
	init: function(){
		this.fmx.parent = this;
	},
	fmx: {
		parent: null,
		notification: function(){
			var opts = {};
			$.extend(opts, {
				
			}, lang.fmx.notification());
			return opts;
		},
		sprite: function(){
			var opts = {};
			$.extend(opts, {
				
			}, lang.fmx.sprite());
			return opts;
		},
		tabs: function(){
			var opts = {};
			$.extend(opts, {
				
			}, lang.fmx.tabs());
			return opts;
		}
	}
});
config.init();